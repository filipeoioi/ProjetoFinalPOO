package projetofinal;

public class Controller {
    public boolean validaLogin(String idUser, String senha){
        boolean retorno;
        int id = Integer.parseInt(idUser);
        //retorno = model.buscarUser(id, senha);
        return retorno;
    }
}
